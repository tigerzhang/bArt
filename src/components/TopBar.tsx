import React from 'react'
import { useCanvasStore, CanvasState } from '../store/canvasStore'

// TopBar has been removed from the default layout; keep a no-op stub
export default function TopBar() {
  return null
}
